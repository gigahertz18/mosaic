name: Deploy to Production

# Placeholder workflow. Deployment infrastructure has not been introduced
# yet; this exists so the pipeline shape is in place from Milestone 1 and
# can be filled in without restructuring CI later.

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy (placeholder)
        run: |
          echo "Production deployment is not yet configured."
          echo "This job will promote a verified staging image to production once deployment infrastructure exists."
